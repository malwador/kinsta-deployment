# Example environment file for local testing
# Copy this to .env.local and fill in your actual Kinsta credentials

# Kinsta sFTP Connection Details (ALL REQUIRED)
KINSTA_HOST_IP=123.456.789.012
KINSTA_USERNAME=your-kinsta-username
KINSTA_PASSWORD=your-kinsta-password
KINSTA_PORT=12345
TARGET_PATH=/www/your-site_123/public

# Deployment Configuration (Optional)
SOURCE_PATH=.
EXCLUDE_PATTERNS=.git,.github,node_modules,.env,.DS_Store,*.log,wp-config-local.php

# Testing Options
DRY_RUN=true
VERBOSE=true

# Kinsta MU Plugin Options
INSTALL_KINSTA_MU_PLUGIN=true
KINSTA_MU_PLUGIN_PATH=wp-content/mu-plugins

# Cache Management
PURGE_KINSTA_CACHE=true

# Notes:
# - KINSTA_HOST_IP: Use the IP address, not the domain (due to Cloudflare)
# - KINSTA_PORT: Each Kinsta site has a unique port number
# - TARGET_PATH: Remote path on Kinsta server (required)
# - Get these values from your Kinsta MyKinsta dashboard -> Sites -> [Your Site] -> Info -> SFTP/SSH
# - Set DRY_RUN=false only when you're ready to actually transfer files
# - Use VERBOSE=true to see detailed transfer information
# - INSTALL_KINSTA_MU_PLUGIN: Set to false to skip Kinsta MU Plugin installation
# - KINSTA_MU_PLUGIN_PATH: Custom path for MU Plugin (for Bedrock use: app/mu-plugins)
# - PURGE_KINSTA_CACHE: Set to false to skip cache purging after deployment
